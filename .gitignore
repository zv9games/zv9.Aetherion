# Build
/target/
**/target/
Cargo.lock
# Keep lock for CLI binary reproducibility once we pin releases:
# (uncomment later if desired) !Cargo.lock

# Godot / GDExtension artifacts — never commit
*.dll
*.so
*.dylib
*.pdb
godot*.exe
Godot_*.exe
Godot_*.zip
.godot/
**/.godot/
*.translation

# OS / IDE
.DS_Store
Thumbs.db
.vs/
.vscode/
.idea/
*.swp
*.user

# Local config secrets
.env
aetherion.local.toml
