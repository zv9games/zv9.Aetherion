/// math_primitives.rs
///
/// This module holds fundamental mathematical constants and definitions shared
/// across the engine, resolving the E0583 error.
/// Currently kept minimal, but ready for expansion (e.g., PI, E, standard epsilon values).

/// The standard size (width, height, depth) of a chunk in tiles.
pub const CHUNK_SIZE: i32 = 16;
